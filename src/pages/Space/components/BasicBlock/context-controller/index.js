import { TodoContext } from "../../../contexts/todos-context";
import { NotepadContext } from "../contexts/notepad-block-context";

const contextController = {
  TodoContext,
  NotepadContext,
};

export default contextController;
